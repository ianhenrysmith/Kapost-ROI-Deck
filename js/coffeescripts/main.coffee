class SlideShowManager
  constructor: () ->
    $(document).pitchdeck({
      link_labels: true
    })

$(document).ready () ->
  # $.stellar({
  #   hideDistantElements: false
  # })
  
  new SlideShowManager

  